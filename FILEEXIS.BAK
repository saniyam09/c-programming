#include<stdio.h>
int main()
{
	FILE*file;

	file = fopen("output.txt","r");
		if(file==NULL){
			printf("file does not exsist\n");
		}else{
			printf("file exsists\n");
			fclose(file);
		}
		return 0;
		}